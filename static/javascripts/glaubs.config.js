(function () {
  'use strict';

  angular
    .module('glaubs.config')
    .config(config);

  config.$inject = ['$locationProvider'];

  /**
  * @name config
  */
  function config($locationProvider) {
  }
})();

